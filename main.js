import * as Phaser from "phaser";

const config = {
  width: 640,
  height: 480,
  scene: [],
  backgroundColor: "#6b8e23",
};

new Phaser.Game(config);

document.title = "Trees vs Hillbillies";
document.body.style.backgroundColor = "#6F4E37";
